# Terraform state
terraform.tfstate
terraform.tfstate.backup
errored.tfstate

# Terraform directories
.terraform/
.terraform.lock.hcl

# Variable overrides (may contain secrets)
*.tfvars
*.tfvars.json
